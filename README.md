# TODO: Containerized-Log-API

Containerized Python FastAPI service that receives log data (JSON) from applications, enriches it with metadata, stores it in Azure Storage, and exposes endpoints to query aggregated results.  The API deploys to Azure Container Apps, scales automatically, and is fully observable with Azure Monitor + Application Insights.
